# CodeCoach AI 前端

这是 CodeCoach AI 的前端应用，基于 React、TypeScript、Vite 和 Ant Design 构建。

## 常用命令

```bash
npm install
npm run dev
npm run build
```

## 本地配置

开发环境默认访问后端接口：

```text
http://localhost:8080
```

如需调整，请修改本地环境变量文件，不要提交敏感配置。
